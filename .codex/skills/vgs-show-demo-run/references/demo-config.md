# Demo Config

Vault id config:
- VGSShowDemoApp/Model/DemoAppConfig.swift
- DemoAppConfig.shared.vaultId defaults to a placeholder value.

The initial screen allows setting the vault id at runtime.
Use sandbox credentials and do not commit real vault ids.
