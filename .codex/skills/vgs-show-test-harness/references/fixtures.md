# JSON Fixtures

Location:
- Tests/VGSShowSDKTests/Resources/JSONs/ValidJSONs

Guidelines:
- Use synthetic values only; never real PII.
- Keep fixture names descriptive and stable.
- Add new files to the bundle helper so tests can load them.
